import MessageBox from '@/components/MessageBox';
import React from 'react';

type Props = {};

function Page({}: Props) {
    return (
        <div className="h-full">
            <MessageBox />
        </div>
    );
}

export default Page;
